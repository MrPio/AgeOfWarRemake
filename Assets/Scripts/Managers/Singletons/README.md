# Singleton scripts

These scripts belong to game objects that live across multiple scenes.
As such it is important to ensure that they don't duplicate when changing scene.

The `Singleton` abstract class implements the *Singleton* design pattern, contextualized to the UnityEngine.